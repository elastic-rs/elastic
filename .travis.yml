language: rust
rust:
  - stable
  - beta
  - nightly
os:
  - linux
  - osx
  - windows
env:
  global:
    - LD_LIBRARY_PATH: /usr/local/lib
script:
  - cd types
  - cargo build -v
  - cargo test -v
  - cargo doc
  - cd ../codegen
  - cargo build -v
  - cargo test -v
  - cargo doc
