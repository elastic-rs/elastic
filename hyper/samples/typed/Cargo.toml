[package]
name = "elastic_hyper_samples"
version = "0.1.0"
authors = ["Ashley Mannix <ashleymannix@live.com.au>"]

[features]
nightly-testing = []

[dependencies]
elastic_hyper = "*"
json_str = { version = "*", features = ["nightly"]}
hyper = "*"
elastic_types = { version = "*", features = ["nightly"], default-features = false }
elastic_types_macros = "*"
serde = "0.8"
serde_macros = "0.8"
serde_json = "0.8"
