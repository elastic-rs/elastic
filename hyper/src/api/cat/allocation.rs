//! http://www.elastic.co/guide/en/elasticsearch/reference/master/cat-allocation.html

//Autogenerated

use hyper::client::Client;
use hyper::header::{Headers, ContentType};
use hyper::client::response::Response;
use hyper::error::Result;

pub fn get_node_id<'a>(client: &'a mut Client, base: String, node_id: String)
 -> Result<Response>{
    let mut url_fmtd = String::with_capacity(base.len() + 17 + node_id.len());
    url_fmtd.push_str(&base);
    url_fmtd.push_str("/_cat/allocation/");
    url_fmtd.push_str(&node_id);
    let mut headers = Headers::new();
    headers.set(ContentType::json());
    let res = client.get(&url_fmtd).headers(headers);
    res.send()
}
pub fn get<'a>(client: &'a mut Client, base: String) -> Result<Response>{
    let mut url_fmtd = String::with_capacity(base.len() + 16);
    url_fmtd.push_str(&base);
    url_fmtd.push_str("/_cat/allocation");
    let mut headers = Headers::new();
    headers.set(ContentType::json());
    let res = client.get(&url_fmtd).headers(headers);
    res.send()
}
